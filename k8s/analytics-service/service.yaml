apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: helep
  labels:
    app: analytics-service
    app.kubernetes.io/part-of: helep
spec:
  selector:
    app: analytics-service
  ports:
    - name: http
      port: 8005
      targetPort: 8005
      protocol: TCP
  type: ClusterIP
