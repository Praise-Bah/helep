apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dispatch-service-data
  namespace: helep
  labels:
    app: dispatch-service
    app.kubernetes.io/part-of: helep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
