apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: helep
  labels:
    app: notification-service
    app.kubernetes.io/part-of: helep
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8004
      targetPort: 8004
      protocol: TCP
  type: ClusterIP
