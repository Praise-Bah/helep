apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sos-service-data
  namespace: helep
  labels:
    app: sos-service
    app.kubernetes.io/part-of: helep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
