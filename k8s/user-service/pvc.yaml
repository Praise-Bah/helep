apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-service-data
  namespace: helep
  labels:
    app: user-service
    app.kubernetes.io/part-of: helep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
