apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: helep
  labels:
    app: user-service
    app.kubernetes.io/part-of: helep
spec:
  selector:
    app: user-service
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
  type: ClusterIP
